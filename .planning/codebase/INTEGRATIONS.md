# External Integrations

**Analysis Date:** 2026-06-13

## APIs & External Services

**NuGet.org:**
- Package publishing target for the PoliNorError NuGet package
- Package URL: https://www.nuget.org/packages/PoliNorError/
- NuGet badge displayed in `README.md` line 2
- `GeneratePackageOnBuild` is `true` in `src/PoliNorError.csproj` line 7, producing `.nupkg` on every build
- No automated publish pipeline detected — no GitHub Actions workflow for publishing exists

**GitHub Repository:**
- Repository URL: https://github.com/kolan72/PoliNorError
- Referenced in `src/PoliNorError.csproj` (`PackageProjectUrl`, `RepositoryUrl`)
- Git repository type specified (`RepositoryType: git`)

## Data Storage

**Databases:**
- Not applicable. Pure library with no data storage.

**File Storage:**
- Not applicable. Local filesystem operations are supported by the library but no file storage integration exists.

**Caching:**
- Not applicable. No caching layer.

## Authentication & Identity

**Auth Provider:**
- Not applicable. Library does not implement authentication.

## Monitoring & Observability

**Error Tracking:**
- Not applicable. Library is itself an error handling tool; no external error tracking integration.

**Code Coverage:**
- Coverage report documented in `CODE_COVERAGE.md`
- Line coverage: 87.6% (5625 of 6419 lines)
- Branch coverage: 82.6% (907 of 1098 branches)
- Parser: Cobertura format
- Generated by ReportGenerator (referenced in `CODE_COVERAGE.md` line 21)
- Coverage generation tool is not configured in the repository — likely run locally or via an external CI system not committed to the repo

**Logs:**
- Not applicable. No logging framework integration. Library uses `System.Diagnostics` for basic diagnostics.

## CI/CD & Deployment

**GitHub Actions:**
- No workflows detected. `.github/workflows/` directory does not exist.

**Dependabot:**
- Configuration: `.github/dependabot.yml`
- Ecosystem: NuGet
- Target branch: `dev`
- Schedule: Weekly on Thursdays at 04:50 America/New_York
- Dependency grouping: NUnit-related packages grouped under `nunit` pattern

**Hosting:**
- NuGet.org — package distribution
- No cloud hosting, containerization, or deployment configuration detected

**Build Artifacts:**
- NuGet package (`.nupkg`) produced on build via `GeneratePackageOnBuild`
- `.nupkg` and `.snupkg` files are git-ignored (`.gitignore` lines 196-198)
- Package icon: `PoliNorError.png` (root directory)
- Package readme: `src/docs/NuGet.md`

## Environment Configuration

**Required env vars:**
- None. No environment variables referenced in source or configuration.

**Secrets location:**
- No secrets or credentials files detected.

**App.config (tests only):**
- `tests/App.config` — Runtime assembly binding redirects for test project
- Redirects for: `System.Threading.Tasks.Extensions`, `Castle.Core`, `NSubstitute`, `nunit.framework`, `nunit.framework.legacy`, `System.Runtime.CompilerServices.Unsafe`, `System.Buffers`, `System.Memory`, `System.Numerics.Vectors`, `System.ValueTuple`
- `gcAllowVeryLargeObjects` enabled for memory-intensive tests

## Test Framework Dependencies

**NUnit 4.5.1:**
- Primary test framework
- Referenced via `tests/packages.config` (legacy NuGet format)
- Props imported in `tests/PoliNorError.Tests.csproj` lines 3-7 (multiple version fallbacks for NUnit 4.2.2 through 4.5.1)
- `nunit.framework` and `nunit.framework.legacy` assemblies referenced

**NUnit3TestAdapter 4.6.0:**
- Visual Studio test adapter for NUnit test discovery and execution
- Referenced via `tests/packages.config`

**NSubstitute 5.3.0:**
- Mocking framework used extensively in tests
- `using NSubstitute;` found in test files (e.g., `tests/RetryPolicyTests.cs` line 8)
- Requires `InternalsVisibleTo("DynamicProxyGenAssembly2")` in `src/PoliNorError.csproj` line 49 to mock internal classes

**Castle.Core 5.2.1:**
- Dynamic proxy library, transitive dependency for NSubstitute
- Explicitly referenced in `tests/packages.config` and `tests/PoliNorError.Tests.csproj`

## Webhooks & Callbacks

**Incoming:**
- Not applicable.

**Outgoing:**
- Not applicable.

## Third-Party Code Attribution

**Polly (BSD 3-Clause):**
- Jittering algorithm for randomized retry delays adapted from [Polly](https://github.com/App-vNext/Polly)
- Attribution: `THIRD-PARTY-NOTICES.txt`
- Files: `src/Retry/StandardJitter.cs`, `src/Retry/ExponentialRetryDelay.DecorrelatedJitter.cs`

---

*Integration audit: 2026-06-13*
