---
phase: 02-runtime-enforcement-in-retry-flow
plan: 01
status: ready
tasks: 5
---

# Plan 01: Stopwatch lifecycle in retry execution

**Status:** Ready to execute

## Tasks

### Task 1: Add elapsed-budget helper method to DefaultRetryProcessor
- Add private static method `CheckElapsedAndFail(PolicyResult result, Stopwatch stopwatch, TimeSpan? limit)`
- Returns `true` if budget exceeded and result marked failed, `false` otherwise
- Uses `result.SetFailed()` when exceeded (no exception, no cancellation)

### Task 2: Add Stopwatch to all RetryInternal sync variants
- Modify all 4 `RetryInternal` sync methods (lines 61-284 in DefaultRetryProcessor.cs)
- Declare `var stopwatch = Stopwatch.StartNew()` at method start
- Check before `HandleException` call (pre-processor)
- Check at top of do-while loop (pre-retry-cycle) before action execution

### Task 3: Add Stopwatch to all RetryInternalAsync variants
- Modify all 4 `RetryInternalAsync` methods (lines 286-495)
- Same pattern as sync variants with async-aware checks
- Use `stopwatch?.Restart()` pattern for consistency

### Task 4: Pass time limit through error saving path
- Modify `HandleException` and `HandleExceptionAsync` in DefaultRetryProcessor.cs
- Check elapsed time before calling `SaveError`/`SaveErrorAsync` or `_saveErrorProcessor.Process`
- Access via `this.ErrorProcessingTimeLimit`

### Task 5: Add NUnit tests for sync Stopwatch start and elapsed retention
- Test that Stopwatch starts before delegate execution
- Test that elapsed time is captured across retry attempts
- Test that time limit check occurs before error processor invocation
