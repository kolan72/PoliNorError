---
phase: 02-runtime-enforcement-in-retry-flow
plan: 03
status: ready
tasks: 3
---

# Plan 03: SetFailed behavior and backward-compatibility tests

**Status:** Ready to execute

## Tasks

### Task 1: Implement SetFailed on time limit exceeded
- Use `result.SetFailed()` (not `SetFailedInner`) per requirement
- No exception attached to PolicyResult
- No cancellation triggered
- FailedReason remains at default (None) or PolicyProcessorFailed if using SetFailedInner

### Task 2: Add sync retry time-limit tests
- Create test with short time limit (e.g., 50ms) and slow delegate (100ms each iteration)
- Verify PolicyResult.IsFailed = true
- Verify retry stopped before completing all attempts
- Verify no cancellation
- Verify no timeout exception

### Task 3: Add async retry time-limit tests and null-bypass verification
- Same pattern as sync tests for async methods
- Verify null ErrorProcessingTimeLimit skips all time checks (existing behavior preserved)
- Verify time limit does not interfere with normal retry flow when not exceeded
