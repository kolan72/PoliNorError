---
phase: 02-runtime-enforcement-in-retry-flow
plan: 02
status: ready
tasks: 4
---

# Plan 02: Elapsed-budget checks before processor and retry-cycle

**Status:** Ready to execute

## Tasks

### Task 1: Add time check before BulkErrorProcessor call
- In `HandleException` (line 569-593), check elapsed time before `DelayProvider.DelayAndCheckIfResultFailed`
- In `HandleExceptionAsync` (line 541-567), same check before async delay
- If exceeded, call `result.SetFailed()` and return false (skip retry increment)

### Task 2: Add time check before retry-cycle continuation
- At top of each do-while loop in all RetryInternal variants
- Check should happen before `action()` execution
- If exceeded, call `result.SetFailed()` and exit loop

### Task 3: Refactor elapsed check into reusable pattern
- Ensure consistent time limit check across all 8 retry variants
- Extract common logic to avoid duplication
- Handle null limit gracefully (skip check)

### Task 4: Add NUnit tests for time budget enforcement
- Test that processor chain stops when time limit exceeded
- Test that retry cycle stops before next iteration
- Verify PolicyResult.IsFailed = true, PolicyResult.IsCanceled = false
