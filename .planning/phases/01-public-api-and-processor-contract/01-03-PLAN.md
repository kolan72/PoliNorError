---
phase: 01-public-api-and-processor-contract
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - tests/RetryPolicyTests.cs
  - tests/DefaultRetryProcessorTests.cs
autonomous: true
requirements:
  - RETRY-EP-01
  - RETRY-EP-06
  - RETRY-EP-07
  - RETRY-EP-08
user_setup: []

must_haves:
  truths:
    - "A .NET consumer can configure a RetryPolicy with `.WithErrorProcessingTimeLimit(TimeSpan)` and the configured total budget is retained by the policy."
    - "A DefaultRetryProcessor constructed with a supplied ErrorProcessingTimeLimit retains the same total budget."
    - "A RetryPolicy or DefaultRetryProcessor without a configured time limit keeps ErrorProcessingTimeLimit null and no timing behavior is enabled."
  artifacts:
    - path: "tests/RetryPolicyTests.cs"
      provides: "integration assertions for policy-to-processor propagation and null default"
      contains:
        - "Should_WithErrorProcessingTimeLimit_Propagate_To_DefaultRetryProcessor"
        - "Should_WithNoErrorProcessingTimeLimit_LeaveProcessorLimitNull"
    - path: "tests/DefaultRetryProcessorTests.cs"
      provides: "constructor contract assertions for DefaultRetryProcessor"
      contains:
        - "Should_ErrorProcessingTimeLimit_StoreSuppliedBudget"
        - "Should_ErrorProcessingTimeLimit_BeNull_ByDefault"
  key_links:
    - from: "tests/RetryPolicyTests.cs"
      to: "src/Retry/DefaultRetryProcessor.cs"
      via: "NUnit casts `RetryProcessor` to `DefaultRetryProcessor` and compares `ErrorProcessingTimeLimit`"
      pattern: "\\(\\(DefaultRetryProcessor\\)policy\\.RetryProcessor\\)\\.ErrorProcessingTimeLimit"
    - from: "tests/DefaultRetryProcessorTests.cs"
      to: "src/Retry/DefaultRetryProcessor.cs"
      via: "constructor parameter stored in internal property"
      pattern: "new DefaultRetryProcessor\\(errorProcessingTimeLimit: limit\\)"

---

<objective>
Close Phase 1 with integration and backward-compatibility verification.

Purpose: Prove RETRY-EP-01, RETRY-EP-06, RETRY-EP-07, and RETRY-EP-08 together: the public fluent API retains the budget, the default processor receives the budget, constructor overloads accept the budget, and null remains disabled with no timing behavior enabled.
Output: Final NUnit integration tests and source assertions across `RetryPolicyTests.cs` and `DefaultRetryProcessorTests.cs`.
</objective>

<execution_context>
@$HOME/.config/kilo/gsd-core/workflows/execute-plan.md
@$HOME/.config/kilo/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-public-api-and-processor-contract/01-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@src/Retry/RetryPolicy.cs
@src/Retry/DefaultRetryProcessor.cs
@tests/RetryPolicyTests.cs
@tests/DefaultRetryProcessorTests.cs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add integration contract tests for policy-to-processor propagation per RETRY-EP-01, RETRY-EP-07, and RETRY-EP-08</name>
  <files>tests/RetryPolicyTests.cs, tests/DefaultRetryProcessorTests.cs</files>
  <read_first>
    - tests/RetryPolicyTests.cs
    - tests/DefaultRetryProcessorTests.cs
    - src/Retry/RetryPolicy.cs
    - src/Retry/DefaultRetryProcessor.cs
    - .planning/phases/01-public-api-and-processor-contract/01-CONTEXT.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Add or adjust integration tests after plans 01-01 and 01-02 are implemented:
    - In `RetryPolicyTests`, add `Should_WithNoErrorProcessingTimeLimit_LeaveProcessorLimitNull`: create `var policy = new RetryPolicy(1);` and assert both policy and default processor `ErrorProcessingTimeLimit` are null.
    - In `RetryPolicyTests`, add `Should_WithErrorProcessingTimeLimit_Propagate_To_DefaultRetryProcessor`: use `var limit = TimeSpan.FromMilliseconds(125); var policy = new RetryPolicy(1).WithErrorProcessingTimeLimit(limit);` and assert `policy.ErrorProcessingTimeLimit == limit` and `((DefaultRetryProcessor)policy.RetryProcessor).ErrorProcessingTimeLimit == limit`.
    - In `DefaultRetryProcessorTests`, add `Should_ErrorProcessingTimeLimit_ConstructedProcessor_KeepsSameBudget`: use `var limit = TimeSpan.FromMilliseconds(250); var processor = new DefaultRetryProcessor(errorProcessingTimeLimit: limit);` and assert the processor property equals `limit`.
    - In `DefaultRetryProcessorTests`, add `Should_ErrorProcessingTimeLimit_BulkProcessorConstructor_KeepsSameBudget`: use `new DefaultRetryProcessor(new BulkErrorProcessor(), false, TimeSpan.FromMilliseconds(75))` and assert the property equals `TimeSpan.FromMilliseconds(75)`.

    These tests compare exact `TimeSpan` values and null defaults. Do not assert retry behavior, elapsed time, or timeout enforcement.
  </action>
  <verify>
    <automated>dotnet test .\tests\PoliNorError.Tests.csproj --no-restore --filter "FullyQualifiedName~PoliNorError.Tests.RetryPolicyTests|FullyQualifiedName~PoliNorError.Tests.DefaultRetryProcessorTests"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/RetryPolicyTests.cs` contains `Should_WithNoErrorProcessingTimeLimit_LeaveProcessorLimitNull`.
    - `tests/RetryPolicyTests.cs` contains `Should_WithErrorProcessingTimeLimit_Propagate_To_DefaultRetryProcessor`.
    - `tests/DefaultRetryProcessorTests.cs` contains `Should_ErrorProcessingTimeLimit_ConstructedProcessor_KeepsSameBudget`.
    - `tests/DefaultRetryProcessorTests.cs` contains `Should_ErrorProcessingTimeLimit_BulkProcessorConstructor_KeepsSameBudget`.
    - All four tests assert exact `TimeSpan` equality or null using `ClassicAssert.AreEqual` / `ClassicAssert.IsNull`.
    - The selected NUnit test command exits with zero failures.
  </acceptance_criteria>
  <done>
    Integration tests prove the configured total budget is retained by `RetryPolicy`, reaches `DefaultRetryProcessor`, and constructor overloads retain the supplied value.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add backward-compatibility gates for null default and no Phase 1 timing behavior per RETRY-EP-06</name>
  <files>tests/RetryPolicyTests.cs, tests/DefaultRetryProcessorTests.cs</files>
  <read_first>
    - tests/RetryPolicyTests.cs
    - tests/DefaultRetryProcessorTests.cs
    - src/Retry/RetryPolicy.cs
    - src/Retry/DefaultRetryProcessor.cs
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-public-api-and-processor-contract/01-CONTEXT.md
  </read_first>
  <action>
    Add or adjust null-default tests:
    - In `RetryPolicyTests`, ensure a new `RetryPolicy(1)` has `ErrorProcessingTimeLimit` null and its default processor has `ErrorProcessingTimeLimit` null.
    - In `DefaultRetryProcessorTests`, ensure `new DefaultRetryProcessor()` and `new DefaultRetryProcessor(errorProcessingTimeLimit: null)` both have null `ErrorProcessingTimeLimit`.

    Add a source assertion gate in verification, not production code, that Phase 1 has not introduced timing enforcement: `src/Retry/RetryPolicy.cs` and `src/Retry/DefaultRetryProcessor.cs` must not contain `Stopwatch` or `Elapsed`.
  </action>
  <verify>
    <automated>dotnet test .\tests\PoliNorError.Tests.csproj --no-restore --filter "FullyQualifiedName~PoliNorError.Tests.RetryPolicyTests|FullyQualifiedName~PoliNorError.Tests.DefaultRetryProcessorTests"</automated>
    <automated>powershell -NoProfile -Command "$files = @('.\src\Retry\RetryPolicy.cs', '.\src\Retry\DefaultRetryProcessor.cs'); foreach ($file in $files) { $lines = Get-Content $file; if (($lines | Select-String 'Stopwatch|Elapsed') -ne $null) { throw "Phase 1 must not add timing enforcement to $file" } }"</automated>
    <automated>powershell -NoProfile -Command "$files = @('.\src\Retry\RetryPolicy.cs', '.\src\Retry\DefaultRetryProcessor.cs'); foreach ($file in $files) { $text = Get-Content $file -Raw; if (-not ($text -match 'internal\s+TimeSpan\?\s+ErrorProcessingTimeLimit')) { throw "Missing internal TimeSpan? ErrorProcessingTimeLimit in $file" } }"</automated>
  </verify>
  <acceptance_criteria>
    - Null-default tests assert `ClassicAssert.IsNull(policy.ErrorProcessingTimeLimit)` for a new `RetryPolicy`.
    - Null-default tests assert `ClassicAssert.IsNull(((DefaultRetryProcessor)policy.RetryProcessor).ErrorProcessingTimeLimit)` for a new `RetryPolicy`.
    - Null-default tests assert `ClassicAssert.IsNull(processor.ErrorProcessingTimeLimit)` for `new DefaultRetryProcessor()` and `new DefaultRetryProcessor(errorProcessingTimeLimit: null)`.
    - The PowerShell `Stopwatch|Elapsed` source assertion exits successfully for both `src/Retry/RetryPolicy.cs` and `src/Retry/DefaultRetryProcessor.cs`.
    - The PowerShell `internal TimeSpan? ErrorProcessingTimeLimit` source assertion exits successfully for both source files.
  </acceptance_criteria>
  <done>
    Backward compatibility is verified: null remains the disabled default, no Phase 1 timing behavior is introduced, and both source files expose the internal nullable state expected by Phase 2.
  </done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NUnit tests → internal members | Tests use `InternalsVisibleTo` to assert internal state without changing public behavior. |
| Policy API → processor state | Integration tests verify the configured value reaches the default processor but do not rely on enforcement. |
| Phase 1 source → Phase 2 enforcement | Tests must not lock in timing enforcement behavior in Phase 1. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Integrity | Null default compatibility | mitigate | Tests assert null for policy and processor defaults. |
| T-01-08 | Tampering | Public-to-internal propagation | mitigate | Integration test casts only the default processor and compares exact `TimeSpan` values. |
| T-01-09 | Repudiation | Phase boundary evidence | mitigate | Source assertion proves no `Stopwatch` or `Elapsed` enforcement was added in Phase 1. |
| T-01-SC | Supply-chain | No new packages | mitigate | No package installs; verification uses existing solution packages. |
</threat_model>

<verification>
- Run `dotnet test .\tests\PoliNorError.Tests.csproj --no-restore --filter "FullyQualifiedName~PoliNorError.Tests.RetryPolicyTests|FullyQualifiedName~PoliNorError.Tests.DefaultRetryProcessorTests"` and require all selected tests to pass.
- Run the PowerShell source assertion that fails if either `src/Retry/RetryPolicy.cs` or `src/Retry/DefaultRetryProcessor.cs` contains `Stopwatch` or `Elapsed`.
- Run the PowerShell source assertion that fails if either source file lacks `internal TimeSpan? ErrorProcessingTimeLimit`.
- Confirm every Phase 1 requirement ID is covered by at least one passing test or source assertion.
</verification>

<success_criteria>
- RETRY-EP-01 is verified by constructor tests retaining the supplied `TimeSpan`.
- RETRY-EP-06 is verified by null-default tests for both `RetryPolicy` and `DefaultRetryProcessor`.
- RETRY-EP-07 is verified by `.WithErrorProcessingTimeLimit(TimeSpan)` retaining and propagating the configured value.
- RETRY-EP-08 is verified by `DefaultRetryProcessor` constructor overload tests.
- Phase 1 does not add timing enforcement.
</success_criteria>

## Artifacts this phase produces
- `RetryPolicy.ErrorProcessingTimeLimit` — internal `TimeSpan?` state, null disabled.
- `RetryPolicy.WithErrorProcessingTimeLimit(TimeSpan)` — public fluent API returning `RetryPolicy`.
- `DefaultRetryProcessor.ErrorProcessingTimeLimit` — internal `TimeSpan?` state, null disabled.
- `DefaultRetryProcessor` constructor plumbing for `TimeSpan? errorProcessingTimeLimit`.
- NUnit tests in `tests/RetryPolicyTests.cs` and `tests/DefaultRetryProcessorTests.cs` proving retention, propagation, constructor acceptance, and null-default compatibility.

<output>
Create `.planning/phases/01-public-api-and-processor-contract/01-03-SUMMARY.md` when done.
</output>
