---
phase: 01-public-api-and-processor-contract
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Retry/RetryPolicy.cs
  - tests/RetryPolicyTests.cs
autonomous: true
requirements:
  - RETRY-EP-06
  - RETRY-EP-07
user_setup: []

must_haves:
  truths:
    - "A .NET consumer can configure RetryPolicy with `.WithErrorProcessingTimeLimit(TimeSpan)`."
    - "The fluent method returns the same `RetryPolicy` instance for chaining."
    - "The configured total budget is retained by the policy and propagated to a DefaultRetryProcessor when present."
    - "RetryPolicy without the fluent call keeps ErrorProcessingTimeLimit null."
  artifacts:
    - path: "src/Retry/RetryPolicy.cs"
      provides: "public fluent API and internal TimeSpan? policy state"
      contains:
        - "WithErrorProcessingTimeLimit(TimeSpan errorProcessingTimeLimit)"
        - "internal TimeSpan? ErrorProcessingTimeLimit"
        - "RetryProcessor is DefaultRetryProcessor"
    - path: "tests/RetryPolicyTests.cs"
      provides: "NUnit coverage for fluent API retention and default null behavior"
      contains:
        - "Should_WithErrorProcessingTimeLimit_BeFluent_AndRetainLimit"
        - "Should_WithErrorProcessingTimeLimit_Propagate_To_DefaultRetryProcessor"
  key_links:
    - from: "src/Retry/RetryPolicy.cs"
      to: "src/Retry/DefaultRetryProcessor.cs"
      via: "WithErrorProcessingTimeLimit assigns the same TimeSpan to DefaultRetryProcessor.ErrorProcessingTimeLimit"
      pattern: "processor\\.ErrorProcessingTimeLimit = errorProcessingTimeLimit"

---

<objective>
Add the RetryPolicy public API and policy-to-processor propagation for error-processing time limits.

Purpose: Implement RETRY-EP-07 while preserving RETRY-EP-06 by adding `.WithErrorProcessingTimeLimit(TimeSpan)` to `RetryPolicy`, storing the value as an internal nullable property, and propagating it to `DefaultRetryProcessor` without breaking custom retry processors.
Output: Public fluent method, internal policy state, propagation behavior, and focused NUnit tests in `tests/RetryPolicyTests.cs`.
</objective>

<execution_context>
@$HOME/.config/kilo/gsd-core/workflows/execute-plan.md
@$HOME/.config/kilo/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-public-api-and-processor-contract/01-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@src/Retry/RetryPolicy.cs
@src/Retry/DefaultRetryProcessor.cs
@src/Retry/IRetryProcessor.cs
@tests/RetryPolicyTests.cs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add RetryPolicy fluent API and internal state per D-01, D-02, D-03, D-05, and RETRY-EP-07</name>
  <files>src/Retry/RetryPolicy.cs, tests/RetryPolicyTests.cs</files>
  <read_first>
    - src/Retry/RetryPolicy.cs
    - src/Retry/DefaultRetryProcessor.cs
    - .planning/codebase/CONVENTIONS.md
    - .planning/codebase/ARCHITECTURE.md
    - .planning/phases/01-public-api-and-processor-contract/01-CONTEXT.md
    - .planning/REQUIREMENTS.md
    - tests/RetryPolicyTests.cs
  </read_first>
  <action>
    Add an internal nullable property to `RetryPolicy` near the existing `Delay` property: `internal TimeSpan? ErrorProcessingTimeLimit { get; set; }`.

    Add a public fluent method near the `WithWait` methods, following D-01 and D-02:
    - Method signature: `public RetryPolicy WithErrorProcessingTimeLimit(TimeSpan errorProcessingTimeLimit)`.
    - The parameter is non-nullable `TimeSpan`, not `TimeSpan?`.
    - The method assigns `ErrorProcessingTimeLimit = errorProcessingTimeLimit;`.
    - The method propagates the value to a default processor with `if (RetryProcessor is DefaultRetryProcessor processor) { processor.ErrorProcessingTimeLimit = errorProcessingTimeLimit; }`.
    - The method returns `this`.

    In the internal `RetryPolicy` constructor that creates `new DefaultRetryProcessor(...)`, set `ErrorProcessingTimeLimit = null;` after `Delay = retryDelay;` so newly constructed policies start with the disabled default. Do not change public constructor signatures in this task.
  </action>
  <verify>
    <automated>dotnet test .\tests\PoliNorError.Tests.csproj --no-restore --filter "FullyQualifiedName~PoliNorError.Tests.RetryPolicyTests"</automated>
  </verify>
  <acceptance_criteria>
    - `src/Retry/RetryPolicy.cs` contains `internal TimeSpan? ErrorProcessingTimeLimit { get; set; }`.
    - `src/Retry/RetryPolicy.cs` contains `public RetryPolicy WithErrorProcessingTimeLimit(TimeSpan errorProcessingTimeLimit)`.
    - `WithErrorProcessingTimeLimit` assigns `ErrorProcessingTimeLimit = errorProcessingTimeLimit;`.
    - `WithErrorProcessingTimeLimit` assigns the same value to `processor.ErrorProcessingTimeLimit` only inside an `if (RetryProcessor is DefaultRetryProcessor processor)` block.
    - `WithErrorProcessingTimeLimit` returns `this` and does not call `ThrowIfProcessorIsNotDefault`.
    - The new RetryPolicy constructor path sets `ErrorProcessingTimeLimit = null;`.
  </acceptance_criteria>
  <done>
    RetryPolicy exposes `.WithErrorProcessingTimeLimit(TimeSpan)`, stores the configured budget internally, propagates it to DefaultRetryProcessor when present, and keeps null as the default.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add NUnit coverage for fluent retention, propagation, custom processor safety, and null default per D-06, D-07, D-08, RETRY-EP-06, and RETRY-EP-07</name>
  <files>tests/RetryPolicyTests.cs</files>
  <read_first>
    - tests/RetryPolicyTests.cs
    - src/Retry/RetryPolicy.cs
    - src/Retry/IRetryProcessor.cs
    - .planning/phases/01-public-api-and-processor-contract/01-CONTEXT.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Add focused NUnit tests inside `RetryPolicyTests`:
    - `Should_ErrorProcessingTimeLimit_BeNull_ByDefault`: create `new RetryPolicy(1)` and assert both `policy.ErrorProcessingTimeLimit` and `((DefaultRetryProcessor)policy.RetryProcessor).ErrorProcessingTimeLimit` are null.
    - `Should_WithErrorProcessingTimeLimit_BeFluent_AndRetainLimit`: create `var policy = new RetryPolicy(1); var limit = TimeSpan.FromSeconds(3); Assert.That(policy.WithErrorProcessingTimeLimit(limit), Is.SameAs(policy)); Assert.That(policy.ErrorProcessingTimeLimit, Is.EqualTo(limit));`
    - `Should_WithErrorProcessingTimeLimit_Propagate_To_DefaultRetryProcessor`: use `TimeSpan.FromMilliseconds(125)`, call `.WithErrorProcessingTimeLimit(limit)`, and assert the default processor's `ErrorProcessingTimeLimit` equals `limit`.
    - `Should_WithErrorProcessingTimeLimit_NotThrow_ForCustomRetryProcessor`: create `new RetryPolicy(new TestRetryProcessor(), 1)`, call `.WithErrorProcessingTimeLimit(TimeSpan.FromMilliseconds(50))`, assert the returned instance is the same policy and `policy.ErrorProcessingTimeLimit` equals the supplied value. Do not expect `NotImplementedException`.

    Use existing test helpers and namespaces from `RetryPolicyTests.cs`. Do not add timing, elapsed, or retry-count assertions.
  </action>
  <verify>
    <automated>dotnet test .\tests\PoliNorError.Tests.csproj --no-restore --filter "FullyQualifiedName~PoliNorError.Tests.RetryPolicyTests"</automated>
    <automated>powershell -NoProfile -Command "$lines = Get-Content '.\src\Retry\RetryPolicy.cs'; if (($lines | Select-String 'Stopwatch|Elapsed') -ne $null) { throw 'Phase 1 must not add timing enforcement to RetryPolicy.cs' }"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/RetryPolicyTests.cs` contains the four test method names listed above.
    - The fluent test asserts `Is.SameAs(policy)` for the `.WithErrorProcessingTimeLimit(limit)` return value.
    - The default-null test asserts `ClassicAssert.IsNull(policy.ErrorProcessingTimeLimit)` and `ClassicAssert.IsNull(((DefaultRetryProcessor)policy.RetryProcessor).ErrorProcessingTimeLimit)`.
    - The propagation test asserts `ClassicAssert.AreEqual(limit, ((DefaultRetryProcessor)policy.RetryProcessor).ErrorProcessingTimeLimit)`.
    - The custom-processor test passes without `Assert.Throws<NotImplementedException>` and without changing the custom processor.
    - The PowerShell source assertion exits successfully with no `Stopwatch` or `Elapsed` matches in `src/Retry/RetryPolicy.cs`.
  </acceptance_criteria>
  <done>
    NUnit tests prove the fluent API is chainable, retains the configured budget, propagates it to DefaultRetryProcessor, does not break custom processors, and preserves null defaults.
  </done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer public API → RetryPolicy fluent method | Callers can configure any non-negative or negative `TimeSpan`; Phase 1 stores the value and leaves validation/enforcement to later phases unless explicitly required. |
| RetryPolicy internal state → DefaultRetryProcessor internal state | The configured value must be copied to the default processor without forcing custom processors to become default processors. |
| Source code → future enforcement | Phase 1 must not introduce timing behavior that changes retry outcomes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | `RetryPolicy.WithErrorProcessingTimeLimit` | mitigate | Use the locked D-01/D-02 signature `TimeSpan errorProcessingTimeLimit`, store the value, and return `this`. |
| T-01-05 | Integrity | Default null behavior | mitigate | Tests assert null for new `RetryPolicy` and its default processor. |
| T-01-06 | Availability | Custom `IRetryProcessor` compatibility | mitigate | Propagate only with `if (RetryProcessor is DefaultRetryProcessor processor)` and do not call `ThrowIfProcessorIsNotDefault` from the fluent method. |
| T-01-SC | Supply-chain | No new packages | mitigate | No package-manager install tasks; verification uses existing solution packages. |
</threat_model>

<verification>
- Run `dotnet test .\tests\PoliNorError.Tests.csproj --no-restore --filter "FullyQualifiedName~PoliNorError.Tests.RetryPolicyTests"` and require all selected tests to pass.
- Run the PowerShell source assertion that fails if `src/Retry/RetryPolicy.cs` contains `Stopwatch` or `Elapsed`.
- Confirm `WithErrorProcessingTimeLimit(TimeSpan)` exists with non-nullable `TimeSpan`, returns the same instance, sets the policy property, and updates only a `DefaultRetryProcessor`.
</verification>

<success_criteria>
- RETRY-EP-07 is satisfied: `RetryPolicy` exposes `.WithErrorProcessingTimeLimit(TimeSpan)` and propagates the configured value to `DefaultRetryProcessor`.
- RETRY-EP-06 is preserved: new policies keep `ErrorProcessingTimeLimit` null unless configured.
- Existing custom retry processor usage is not broken by the new fluent method.
- No Phase 1 timing enforcement is introduced.
</success_criteria>

## Artifacts this phase produces
- `RetryPolicy.ErrorProcessingTimeLimit` — internal `TimeSpan?` state, null disabled.
- `RetryPolicy.WithErrorProcessingTimeLimit(TimeSpan)` — public fluent API returning `RetryPolicy`.
- Default-processor propagation from `RetryPolicy.WithErrorProcessingTimeLimit` to `DefaultRetryProcessor.ErrorProcessingTimeLimit`.
- NUnit tests in `tests/RetryPolicyTests.cs` for fluent retention, propagation, custom processor safety, and null default.
- Earlier/later in this phase, `DefaultRetryProcessor` constructor plumbing and integration tests complete the phase contract.

<output>
Create `.planning/phases/01-public-api-and-processor-contract/01-02-SUMMARY.md` when done.
</output>
