---
phase: 01-public-api-and-processor-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Retry/DefaultRetryProcessor.cs
  - tests/DefaultRetryProcessorTests.cs
autonomous: true
requirements:
  - RETRY-EP-01
  - RETRY-EP-08
user_setup: []

must_haves:
  truths:
    - "DefaultRetryProcessor can be constructed with an optional ErrorProcessingTimeLimit and retains the supplied total budget."
    - "DefaultRetryProcessor keeps ErrorProcessingTimeLimit null when no budget is supplied."
    - "Phase 1 adds processor contract state without adding runtime timing enforcement."
  artifacts:
    - path: "src/Retry/DefaultRetryProcessor.cs"
      provides: "internal TimeSpan? ErrorProcessingTimeLimit state and constructor plumbing"
      contains:
        - "internal TimeSpan? ErrorProcessingTimeLimit"
        - "TimeSpan? errorProcessingTimeLimit = null"
    - path: "tests/DefaultRetryProcessorTests.cs"
      provides: "NUnit coverage for processor constructor contract and null default"
      contains:
        - "Should_ErrorProcessingTimeLimit_BeNull_ByDefault"
        - "Should_ErrorProcessingTimeLimit_StoreSuppliedBudget"
  key_links:
    - from: "tests/DefaultRetryProcessorTests.cs"
      to: "src/Retry/DefaultRetryProcessor.cs"
      via: "NUnit assertions against internal ErrorProcessingTimeLimit"
      pattern: "processor\\.ErrorProcessingTimeLimit"

---

<objective>
Add the processor-side contract for RetryPolicy error-processing time limits.

Purpose: Implement RETRY-EP-01 and RETRY-EP-08 by giving `DefaultRetryProcessor` an optional `TimeSpan? ErrorProcessingTimeLimit`, with null preserved as the backward-compatible disabled state per D-03 and D-04.
Output: Processor state, constructor overloads, and focused NUnit tests in `tests/DefaultRetryProcessorTests.cs`.
</objective>

<execution_context>
@$HOME/.config/kilo/gsd-core/workflows/execute-plan.md
@$HOME/.config/kilo/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-public-api-and-processor-contract/01-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@src/Retry/DefaultRetryProcessor.cs
@src/Retry/DefaultRetryProcessor.RetryOverloads.cs
@src/Retry/DefaultRetryProcessor.RetryOverloads.RetryDelay.cs
@tests/DefaultRetryProcessorTests.cs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add DefaultRetryProcessor time-limit state and constructor plumbing per D-03, D-05, D-06, D-07, and D-08</name>
  <files>src/Retry/DefaultRetryProcessor.cs</files>
  <read_first>
    - src/Retry/DefaultRetryProcessor.cs
    - src/Retry/DefaultRetryProcessor.RetryOverloads.cs
    - src/Retry/DefaultRetryProcessor.RetryOverloads.RetryDelay.cs
    - .planning/codebase/CONVENTIONS.md
    - .planning/phases/01-public-api-and-processor-contract/01-CONTEXT.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Add an internal nullable time-limit property to `DefaultRetryProcessor` near `_delayProvider`: `internal TimeSpan? ErrorProcessingTimeLimit { get; set; }`.

    Extend the public constructor chain without changing the existing call sites:
    - `public DefaultRetryProcessor(bool failedIfSaveErrorThrows = false, TimeSpan? errorProcessingTimeLimit = null) : this(null, failedIfSaveErrorThrows, null, errorProcessingTimeLimit) { }`
    - `public DefaultRetryProcessor(IBulkErrorProcessor bulkErrorProcessor, bool failedIfSaveErrorThrows = false, TimeSpan? errorProcessingTimeLimit = null) : this(bulkErrorProcessor, failedIfSaveErrorThrows, null, errorProcessingTimeLimit) { }`
    - `internal DefaultRetryProcessor(IDelayProvider delayProvider, TimeSpan? errorProcessingTimeLimit = null) : this(null, false, delayProvider, errorProcessingTimeLimit) { }`
    - Extend the main internal constructor to `internal DefaultRetryProcessor(IBulkErrorProcessor bulkErrorProcessor, bool failedIfSaveErrorThrows, IDelayProvider delayProvider = null, TimeSpan? errorProcessingTimeLimit = null)` and assign `ErrorProcessingTimeLimit = errorProcessingTimeLimit;` inside it.

    Do not add `Stopwatch`, elapsed-time checks, `PolicyResult.SetFailed()` calls, cancellation behavior, or enforcement logic in this plan. This plan is only the processor contract and state storage for Phase 1.
  </action>
  <verify>
    <automated>dotnet test .\tests\PoliNorError.Tests.csproj --no-restore --filter "FullyQualifiedName~PoliNorError.Tests.DefaultRetryProcessorTests"</automated>
  </verify>
  <acceptance_criteria>
    - `src/Retry/DefaultRetryProcessor.cs` contains exactly one internal property named `ErrorProcessingTimeLimit` with type `TimeSpan?`.
    - `src/Retry/DefaultRetryProcessor.cs` contains constructor signatures with `TimeSpan? errorProcessingTimeLimit = null`.
    - The main `DefaultRetryProcessor` constructor assigns `ErrorProcessingTimeLimit = errorProcessingTimeLimit;`.
    - No `Stopwatch` or `Elapsed` references are added to `src/Retry/DefaultRetryProcessor.cs`.
  </acceptance_criteria>
  <done>
    DefaultRetryProcessor stores `TimeSpan? ErrorProcessingTimeLimit`, accepts the value through constructor plumbing, and remains null by default with no timing enforcement added.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add NUnit coverage for DefaultRetryProcessor null-disabled and supplied-budget contracts per D-03, D-04, RETRY-EP-01, and RETRY-EP-06</name>
  <files>tests/DefaultRetryProcessorTests.cs</files>
  <read_first>
    - tests/DefaultRetryProcessorTests.cs
    - src/Retry/DefaultRetryProcessor.cs
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-public-api-and-processor-contract/01-CONTEXT.md
  </read_first>
  <action>
    Add focused NUnit tests inside `DefaultRetryProcessorTests`:
    - `Should_ErrorProcessingTimeLimit_BeNull_ByDefault`: create `new DefaultRetryProcessor()` and assert `processor.ErrorProcessingTimeLimit` is null.
    - `Should_ErrorProcessingTimeLimit_StoreSuppliedBudget`: create `var limit = TimeSpan.FromMilliseconds(250); var processor = new DefaultRetryProcessor(errorProcessingTimeLimit: limit);` and assert the property equals `limit`.
    - `Should_ErrorProcessingTimeLimit_StoreNull_WhenExplicitlyNull`: create `new DefaultRetryProcessor(errorProcessingTimeLimit: null)` and assert the property is null.
    - `Should_ErrorProcessingTimeLimit_StoreSuppliedBudget_WithBulkProcessor`: create `new DefaultRetryProcessor(new BulkErrorProcessor(), false, TimeSpan.FromMilliseconds(125))` and assert the property equals the supplied value.

    Use `ClassicAssert.IsNull` for null checks and `ClassicAssert.AreEqual` for supplied `TimeSpan` values. Do not assert retry counts, elapsed time, or retry behavior in these tests.
  </action>
  <verify>
    <automated>dotnet test .\tests\PoliNorError.Tests.csproj --no-restore --filter "FullyQualifiedName~PoliNorError.Tests.DefaultRetryProcessorTests"</automated>
    <automated>powershell -NoProfile -Command "$lines = Get-Content '.\src\Retry\DefaultRetryProcessor.cs'; if (($lines | Select-String 'Stopwatch|Elapsed') -ne $null) { throw 'Phase 1 must not add timing enforcement to DefaultRetryProcessor.cs' }"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/DefaultRetryProcessorTests.cs` contains the four test method names listed above.
    - The default-processor test asserts `ClassicAssert.IsNull(processor.ErrorProcessingTimeLimit)`.
    - The supplied-budget tests assert `ClassicAssert.AreEqual(limit, processor.ErrorProcessingTimeLimit)` or equivalent exact `TimeSpan` equality.
    - The explicit-null test asserts `ClassicAssert.IsNull(processor.ErrorProcessingTimeLimit)`.
    - The PowerShell source assertion exits successfully with no `Stopwatch` or `Elapsed` matches in `src/Retry/DefaultRetryProcessor.cs`.
  </acceptance_criteria>
  <done>
    NUnit tests prove `DefaultRetryProcessor` accepts and retains an optional error-processing budget while preserving null as the disabled default.
  </done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer public API → DefaultRetryProcessor constructor | Callers can supply `TimeSpan? errorProcessingTimeLimit`; invalid runtime behavior must be limited to storing the value. |
| DefaultRetryProcessor internal state → future retry flow | Internal `ErrorProcessingTimeLimit` is a state carrier for Phase 2 and must not trigger enforcement in Phase 1. |
| Package restore/build | No new packages are introduced; existing test packages are used only by verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `DefaultRetryProcessor` constructor parameter | mitigate | Use `TimeSpan? errorProcessingTimeLimit = null` and store the exact value in `ErrorProcessingTimeLimit`. |
| T-01-02 | Integrity | Null default behavior | mitigate | Add NUnit tests that assert null for `new DefaultRetryProcessor()` and explicit-null construction. |
| T-01-03 | Tampering | Phase boundary | mitigate | Do not add `Stopwatch`, elapsed checks, or `SetFailed()` behavior in Phase 1; verify with a source assertion. |
| T-01-SC | Supply-chain | NuGet/test packages | mitigate | No package installs in this plan; verification uses already-present solution packages. |
</threat_model>

<verification>
- Run `dotnet test .\tests\PoliNorError.Tests.csproj --no-restore --filter "FullyQualifiedName~PoliNorError.Tests.DefaultRetryProcessorTests"` and require all selected tests to pass.
- Run the PowerShell source assertion that fails if `src/Retry/DefaultRetryProcessor.cs` contains `Stopwatch` or `Elapsed`.
- Confirm `ErrorProcessingTimeLimit` is declared as `TimeSpan?` and constructor plumbing passes the exact `errorProcessingTimeLimit` parameter into the main constructor.
</verification>

<success_criteria>
- RETRY-EP-01 is satisfied for the processor contract: `DefaultRetryProcessor` accepts an optional `TimeSpan? errorProcessingTimeLimit`.
- RETRY-EP-08 is satisfied: the constructor overload path stores the supplied value on the processor.
- RETRY-EP-06 is preserved for the processor: null remains the disabled default.
- Phase 1 timing enforcement is not introduced.
</success_criteria>

## Artifacts this phase produces
- `DefaultRetryProcessor.ErrorProcessingTimeLimit` — internal `TimeSpan?` state, null disabled.
- `DefaultRetryProcessor` constructor plumbing for `TimeSpan? errorProcessingTimeLimit`.
- NUnit tests in `tests/DefaultRetryProcessorTests.cs` for null default, explicit null, and supplied-budget retention.
- Later in this phase, `RetryPolicy.ErrorProcessingTimeLimit`, `RetryPolicy.WithErrorProcessingTimeLimit(TimeSpan)`, and propagation tests will complete the public API contract.

<output>
Create `.planning/phases/01-public-api-and-processor-contract/01-01-SUMMARY.md` when done.
</output>
